# Clean sample project

Instructions that pass every agentsmd rule.

## Build

Run the build script in `scripts/` after every dependency change.

## Tests

The suite lives in `tests/` -- keep it green before pushing.

## Layout

Rules for this directory live in `AGENTS.md`.
